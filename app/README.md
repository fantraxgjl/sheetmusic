# Piano Improv (app)

The application itself. See [`../README.md`](../README.md) for what this is,
requirements, and install/deploy instructions, and [`../PLAN.md`](../PLAN.md)
for the full design rationale and build history.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build      # outputs to dist/
npm run preview    # serve the build locally
```
